# Diagram Export Limitation

Mermaid CLI (mmdc) is not installed in this environment, so the foundation build generated editable .mmd sources only. Install Mermaid CLI to export SVG/PNG without changing the source diagrams.
